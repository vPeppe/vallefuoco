clear() 
drawStage(ColorMaker.yellow) 
class Quadrato(x: Int, y: Int) { 
 val pic = Picture.rectangle(60, 60) 
 pic.setFillColor(black) 
 pic.setPosition(x, y) 
 var vel = Vector2D(2, 10) 
 def draw() { 
 pic.draw() 
 } 
 
}   //> class Quadrato
val br = new Quadrato(-100, -100)   //> val br: Quadrato = Quadrato@5298e7ac
br.draw() 
